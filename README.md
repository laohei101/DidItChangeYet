# DidItChangeYet
A single-binary CLI tool and background daemon that monitors any URL or API endpoint for changes in a specific JSON field or HTML element (CSS selector), and sends alerts via Telegram, email (SMTP), or ntfy when a user-defined condition is met. Designed to be self-hosted, configurable via a simple YAML file, and robust for 24/7 operation.
